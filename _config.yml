name: Cultivate Software Ltd, Staff Handbook

exclude: ['config.ru', 'Gemfile', 'Gemfile.lock', 'gen.rb', 'Procfile', 'Rakefile', 'README.md']
markdown: redcarpet
redirects: true
baseurl: "/ukstaffhandbook"


defaults:
  -
    scope:
      path: "pages"
    values:
      layout: "page"
